#[cfg(test)]
mod tests {
    #[test]
    fn test_write_file() {
        assert!(true);
    }
    
    #[test]
    fn test_create_file() {
        assert!(true);
    }
    
    #[test]
    fn test_delete_file() {
        assert!(true);
    }
    
    #[test]
    fn test_allocate_inode() {
        assert!(true);
    }
    
    #[test]
    fn test_allocate_block() {
        assert!(true);
    }
}
