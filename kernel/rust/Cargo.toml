[package]
name = "wishy_kernel"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
spin = { version = "0.9", default-features = false, features = ["spin_mutex"] }
bitflags = "2.4"

[profile.release]
panic = "abort"
lto = true
opt-level = 2
